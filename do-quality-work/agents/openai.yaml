interface:
  display_name: "Do Quality Work"
  short_description: "Design, plan, and verify complex work"
  default_prompt: "Use $do-quality-work to choose the required architecture and plan depth, create a durable checkpoint plan when needed, and verify the smallest safe implementation path."
